Little script to poll beer tasting events from Taysta.com

`run.sh` is a crontab-friendly wrapper script to simplify automation.

#### Build
```
gradlew jar
```

#### Test run
```
java -jar build/libs/beertasting-finder-0.0.2.jar
```
